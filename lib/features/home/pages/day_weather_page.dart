import 'package:flutter/material.dart';

class DayWeatherPage extends StatelessWidget {
  const DayWeatherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
